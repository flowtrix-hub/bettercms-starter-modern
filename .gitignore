node_modules
.next
.env
.env.local
next-env.d.ts
*.tsbuildinfo
bcms-content.json

# static export output
out

# A timestamped backup (.env.bak.<ts>) slipped past a bare `.env` rule once and committed a
# live GitHub token — FLO-675. Ignore every variant; keep the checked-in example.
.env
.env.*
!.env.example
